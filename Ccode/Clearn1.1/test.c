#define _CRT_SECURE_NO_WARNINGS

#include <stdio.h>//#是指令形式，include指包含，包含一个叫stdio.h的文件
//stdio名称由来：std-标准standard，i：input,o:output  以后头文件都得这样

/*int main()  //主函数-程序的入口，main函数有且仅有一个，main前面的int表示main函数调用后返回一个整型值
{
	//这里完成任务，在屏幕上输出hello world（print function），printf指打印函数
	double d = 3.14;
	printf("%lf\n", d);

	return 0;  //return是返回
}
/*这种写法是过时的写法
void main(){
}
*/
